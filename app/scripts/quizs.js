function loadQuizs() {	// dis but	$.getJSON('quizs.php', {		request: 'loadQuizs'	}, function(quizs) {		if (quizs.length > 0) {			$.each(json.questions, function(i, quiz) {				addQuiz(quiz);			});		} else {			$('#quizs').html('<strong>Y u no create quizzess?!</strong><br /><img src="images/pearcat.jpg" />');		}		$('#title').val('');	})	.complete(function(){		// en but	});}function addQuiz(quiz) {	}